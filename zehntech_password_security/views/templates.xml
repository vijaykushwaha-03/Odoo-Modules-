<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <template id="auth_login_inherit" inherit_id="web.login" name="Login Inherit">
            <xpath expr="//div[hasclass('mb-3')][3]" position="replace">
                <div class="mb-3 field-password">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" placeholder="Password" name="password" id="password" t-attf-class="form-control #{'form-control-sm' if form_small else ''}" required="required" autocomplete="current-password" t-att-autofocus="'autofocus' if login else None" maxlength="4096"/>
                    <span class="fa fa-eye-slash" id="b2b_toggle_password"></span>
                </div>
            </xpath>
        </template>

        <template id="auth_signup_inherit" inherit_id="auth_signup.fields" name="Signup Inherit">
            <div class="mb-3 field-password pt-2" position="replace">
                <div class="mb-3 field-password pt-2">
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" class="form-control form-control-sm"
                           required="required" t-att-autofocus="'autofocus' if only_passwords else None"/>
                    <span class="fa fa-eye-slash" id="b2b_toggle_password"></span>
                </div>
            </div>
            <div class="mb-3 field-confirm_password" position="replace">
                <div class="mb-3 field-confirm_password">
                    <label for="confirm_password">Confirm Password</label>
                    <input type="password" name="confirm_password" id="confirm_password"
                           class="form-control form-control-sm" required="required"/>
                    <span class="fa fa-eye-slash" id="b2b_toggle_confirm_password"></span>
                </div>
            </div>
        </template>
    </data>
</odoo>